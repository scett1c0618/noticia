@model IEnumerable<noticia.Services.Post>

@{
    ViewData["Title"] = "Noticias";
}

<h1>Noticias</h1>

<ul>
@foreach (var post in Model)
{
    <li>
        <a href="@Url.Action("Details", "Posts", new { id = post.Id })">
            @post.Title
        </a>
    </li>
}
</ul>